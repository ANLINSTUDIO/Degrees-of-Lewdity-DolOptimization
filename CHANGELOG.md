# 原版优化模组 (Dol-Optimization) 更新日志

本项目遵循语义化版本规范，所有界面文字、代码注释与交互均严格遵循 Degrees of Lewdity 原生暗黑冒险风格与规范简体中文标准。

## v1.1.0.5 - 2026-09-23

### 新功能
- 模组管理器新增配置保存状态提示，区分正在保存、已保存且重载后生效、操作失败与状态待核实。
- 新增“刷新列表”按钮，便于在原版管理器调整配置后重新同步模组列表和资料。

### 优化性更新
- 重复打开管理器时复用内存列表与禁用模组资料缓存；环境统计直接读取同一份配置，减少重复读取与解析。导入或升级后同步资料，删除后移除对应缓存记录。
- 启用、禁用及排序先更新界面，再异步保存。模组、美化、拖拽、导入与安全模式共用操作保护，保存期间限制冲突操作和提前重载。

### 漏洞修复
- 修复模组配置保存失败仍显示操作成功的问题。写入失败后重新读取实际配置；无法核实时明确提示并阻止继续覆盖，刷新成功后恢复操作。
- 修复美化配置保存前提前更新插件内部列表的问题，并处理模组列表部分写入失败后的状态校准。
- 修复模组市场忽略导入失败结果、仍将版本记为已更新的问题。

## v1.1.0.4 - 2026-09-23

### 漏洞修复
- 修复模组市场将 GitHub 上仅用于备份的 Tag 误判为最新发布版本的问题。索引现在以 GitHub 正式 Release 为准；当 Release 使用 `Pre-release` 等通用 Tag 时，从发布名称提取版本号。
- 修复部分模组显示“更新未知”的问题。优先显示 GitHub Release 的真实发布日期；抓取失败时保留同一 Release 已核实的日期，再回退至中文 Wiki 的日期。Wiki 未提供日期的模组不显示“更新”字段。

## v1.1.0.3 - 2026-09-22

### 1. 核心与交互修复
- **对齐原版 ModLoader 权威加载日志与统计规范（1 错误、1 警告）**：
  - **纠正重复段落名误升格为错误**：修正 `Passout park` 等段落重名（`CacheRecord.replaceMerge() has duplicate name`）在原版被打上 `[[logWarning]]` 的真实级别，将 `duplicate name` 从错误规则移入警告规则，精准还原为【1 处警告】；
  - **控制台与内部日志跨流智能去重**：针对 ModLoader 在版本不符（`DependenceChecker.checkGameVersion() not satisfies`）时同时向控制台和内部日志双重输出的特性，实现智能跨流去重，消除同一错误被双重统计的问题，使统计结果与原版 ModLoader 权威数字完全对齐；
  - **修复顶栏标签高亮与阴影偏差**：将 `modloader.twee` 中 widget 参数修正为 SugarCube 规范的临时变量 `_args[0]`（杜绝误用故事全局变量 `$args[0]`），确保呼出【加载日志】时顶栏对应标签具备原生 `tab-selected` 高亮与白色阴影；
  - **杜绝无错误时误触发报错弹窗**：在控制台拦截器中将本地 `file://` 协议下读取 `modList.json` 预期的良性 CORS 降级加入过滤白名单，防止无错误时误弹窗；
  - **加载生命周期保护**：将兜底强制弹窗时限从 22 秒延长至 45 秒，优先依赖 `:storyready` 权威就绪事件，彻底消除模组尚未完全解压完成就提前弹出半成品日志的不一致现象。
- **启动检测到加载错误时自动弹窗报错页彻底修复**：
  - **根除反向关闭缺陷**：重构 `dolOptOpenManager` 呼出机制，彻底移除调用原版 `<<overlayReplace "modloader">>` 造成的当前同名弹窗反向触发 `<<closeOverlay>>` 的历史隐患；
  - **杜绝面板双重渲染竞态**：改用底层安全 DOM 显示结合精准 Twine 宏替换，杜绝了模组管理与加载日志在同一个生命周期中互相冲刷、导致页面空白或定位失效的问题；
  - **启动遮罩期智能挂起与精准消费**：在游戏启动早期（存在 `#init-screen` 且 `#customOverlay` 尚未挂载）将弹窗意图优雅挂起为 pending 状态，并在通道就绪（`:storyready`、`:passagedisplay` 或延时自检）的第一时间精准唤醒弹窗并平滑定位至首个错误行，且全局生命周期仅弹窗一次不骚扰玩家。

### 2. 网络与下载链路修复与升级
- **模组市场国内直连下载线路与容灾体系全面重构**：
  - **废除强制改走 Worker 的历史死穴**：彻底剔除无官方摘要（`!assetDigest`）时强行将用户所选加速镜像改走 Worker 的缺陷，解开导致国内直连频繁超时报错（`ERR_CONNECTION_TIMED_OUT`）的核心根因；
  - **实测极速节点重构线路梯队**：通过国内真实网络压测实测，淘汰已被连接重置阻断的 LLKK 节点（`ERR_CONNECTION_RESET`），全新引入实测 1.2 秒响应、CORS 完备的【加速通道 2（Boki 高速）】；并将实测低延迟稳定的【加速通道 1（DDLC 加速·推荐）】设为默认首选；
  - **首创跨镜像自动故障转移（Multi-Mirror Auto-Failover）**：引入多镜像链路自愈容灾机制与 8 秒连接握手超时熔断，当首选镜像遭遇网络波动或节点故障时，前端自动且静默切换至下一个可用加速通道继续下载并完成安装，用户无感知享受 99.9% 下载成功率；
  - **向后平滑兼容映射**：全面支持旧版 `jasonzeng`、`ghfast`、`llkk` 等持久化线路配置平滑映射至最新可用节点。

## v1.1.0.2 - 2026-09-22

### 1. 优化性更新
- **下载线路全面重构与梯队智能升级**：
  - **自建高速专线置为首选默认**：将实测秒级极速（658ms 响应，速率达 448 KB/s）的自建专线提升为市场首选默认线路【加速通道 1（高速专线）】，玩家免切换即可享受飞速下载。
  - **淘汰瘫痪节点并引入全新镜像**：彻底淘汰目前已被其服务商 100% 触发 HTTP 429 报错的 DDLC 节点，引入国内极速公共加速节点【加速通道 3（GHFast）】。
  - **经典高可用备用通道**：将具备完整 CORS 支持的公共镜像保留为【加速通道 2（JasonZeng）】，构成“自建高速专线 + 经典公共通道 + GHFast + GitHub 直连”四级高可用下载体系。
  - **智能容灾与自愈链路**：优化镜像线路下载容错逻辑，当无官方摘要触发 Worker 安全代理而遇到网络波动时，自动且无缝回退使用玩家首选的加速镜像下载，彻底解决直连不用 VPN 时的下载报错。
  - **Release 解析缓存兜底**：模组市场 Release 获取流程增强，当直连 GitHub API 受网络波动阻断时，自动回退使用最近成功缓存的 Release 资产数据，保障国内玩家不用 VPN 也能稳定解析并下载最新模组包。
  - **增强无安装包友好提示**：当模组最新发布仅包含源码或作者尚未上传压缩包时，明确给出指引，避免误报为镜像故障。
- **移动端一键截图体验全面升级**：
  - 针对移动端浏览器与 WebView 禁止异步静默下载或无法写入图片剪贴板的问题，新增移动端长图预览对话框。
  - 支持手机用户长按图片直接将诊断长图保存至手机相册，兼备主动下载按钮与双重安全兜底，桌面端则保持原有一键复制到剪贴板与自动保存功能。

### 2. 漏洞修复
- **彻底根治 Toast 提示框高度过高、收不回去穿帮的样式缺陷**：
  - 修复 Toast 隐藏机制仅依赖向下平移而缺少 `opacity: 0` 与 `visibility: hidden` 的历史问题，彻底消除在手机窄屏上长文本折行后半截卡在屏幕底部、遮挡底部版本号与状态栏的穿帮现象。
  - 精简 Toast 文本内容，并在移动端响应式布局中优化安全底边距，入场与退场实现平滑优雅的无痕淡入淡出。

## v1.1.0.1 - 2026-09-21

### 1. 优化性更新
- **模组市场主备双轨网络容灾**：全面引入高可用双镜像智能轮询链路，默认优先走国内免翻高速专线（`https://dol.alseece.top`，延迟仅 30~50ms），若遇网络故障或维护，自动无缝秒级切换至 Cloudflare 官方 Worker 兜底，双重保障全网玩家免挂 VPN 顺畅加载市场。
- **动态活跃路由智能感知**：在成功探测并命中通畅节点后，后续查看模组说明文档（`/readme`）与代理下载安装包（`/download`）自动联动使用该通畅节点。
- **移动端视觉与布局调优**：修复移动端 Tab 标签页显示与连通问题，模组管理与模组市场同步使用舒适的网格排布，优化一键更新按钮宽度与加载日志按钮在窄屏下的防挤压排布。
- **作者呈现信息更新**：模组作者呈现信息中正式同步加入 `JohnLiao`。

### 2. 漏洞修复
- **修复国内网络环境下的模组市场加载失败与版本异常误报**：彻底根除因官方 `workers.dev` 域名在国内受阻导致的回退异常（99 款未清洗旧数据与织境空间版本误报）以及移动端 `Failed to fetch` 彻底打不开的严重缺陷。

## v1.1.0 - 2026-09-20

### 1. 新功能
- **下载前依赖预检**：安装模组前递归检查缺失、版本不足或已禁用的前置模组，并使用项目现有暗色确认框提示一并处理。
- **依赖联动安装**：用户确认后按依赖优先顺序下载或更新前置模组，再继续安装目标模组；取消时仅安装目标模组。
- **模组分类完整收敛**：为复核确认的 15 个模组补充权威主分类与内容标签；市场仅收录 98 个真实模组，当前待分类数量归零。
- **上下文分类筛选**：分类胶囊只在社区全部模组视图显示；切换到【本地已装】或【发现新版】时自动清除隐藏分类条件，避免筛选状态与界面不一致。
- **已忽略模组独立列表**：市场工具栏新增【查看已忽略模组】入口并显示数量；列表内仍可直接更新，也可取消忽略并恢复当前版本提醒。
- **加载日志全屏最大化展示**：新增全屏模式切换按钮（支持 `Esc` 键快捷还原），弹窗与日志内容自适应铺满全屏，单屏可见日志行数提升数倍，大幅减少滚屏与翻看成本。
- **日志级别精准筛选器**：工具栏集成【全部】、【仅错误】、【仅警告】与【错误+警告】胶囊筛选器，瞬间过滤隐藏无关的正常加载信息，关键报错紧密聚合，彻底消除截图杂音。
- **原生“一键截图”长图引擎**：采用纯原生 Canvas 实时生成高保真暗黑终端风格报错诊断长图（包含游戏版本、ModLoader 版本、错误总数与自动折行报错列表），支持**直接写入系统剪贴板（可直接在贴吧/交流群 Ctrl+V 粘贴发送）**并自动保存 PNG 图片文件。

### 2. 优化性更新
- **依赖下载进度可视化**：安装前置模组时，在用户点击的目标模组卡片同步显示分阶段下载动画、实时百分比与安装状态；前置模组卡片可见时仍保留自身进度。
- **依赖数据统一下发**：发布索引、静态模组目录与客户端缓存统一保留依赖声明，并通过新版缓存键与 Worker 索引键即时刷新旧数据。
- **依赖目录首批补齐**：补充简易框架，以及公交车防骚扰、更长遭遇战／言灵作弊集、泰拉瑞亚拓展对秋枫白桦框架的依赖关系。
- **身份表与索引同步扩充**：权威身份目录扩充至 45 条，源文件、静态站点与 Worker 统一使用同一份仓库身份、主分类和内容标签数据。
- **模组名称标准化增强**：解析 Wiki 与身份目录时统一还原常见 HTML 实体，带有 `&amp;` 等编码的模组名称也能正确匹配。
- **部署后即时重建索引**：升级 Worker 索引缓存版本，首次访问时自动基于最新身份目录重新生成分类结果。
- **模组市场排版收紧**：桌面端分类改为四列两行，状态、排序与下载线路等宽排列；窄屏自动切换为两列分类和单列控件。
- **更新状态语义明确**：以【已忽略本次】【已永久忽略】【取消忽略】替代含义模糊的“标为最新 / 恢复检测”，避免把被忽略的更新误解为本地已经升级。
- **深度优化模组加载日志全屏视图在手机与移动端的排版适配**：全屏状态下隐藏冗长诊断卡片与大标题，最大化释放视距（日志可视面积达 85% 以上），超薄操作顶栏平分整行触控舒适，确保手机端随时一键还原退出。
- **规范化安装包产物归档流程**：打包脚本严格输出至 release 归档目录与 MOD 游戏目录，杜绝根目录残留包体。
- **市场数据来源公开**：模组市场页明确标注资料来自 DOL 中文社区 Wiki，并提供可直接访问的【模组列表】链接。
- **安装确权与用户忽略分离**：市场安装后的版本确权使用独立记录，仅用于防止第三方安装包内部版本号滞后造成循环更新，不再伪装成玩家主动忽略。
- **说明文档统一展示市场资料与仓库入口**：无论模组是否包含内置 ReadMe，只要市场有收录资料，均在页面顶部统一呈现社区中文通俗简介、主分类标签与【访问模组仓库】快捷按钮；有 ReadMe 的模组在文末同步呈现依赖项与扩展插件技术规格，消除信息割裂。

### 3. 漏洞修复
- **修复依赖提示未生效**：重新打包同版本安装包，确保 v1.1.0 实际包含依赖预检代码；同时修复秋枫白桦框架相关模组缺少依赖字段的问题。
- **修复批量更新误计成功**：安装失败的模组不再计入批量更新成功数量。
- **修复开发工具混入模组市场**：正确识别 Wiki 二级标题边界，不再把模组加载器、开发助手、打包工具等 9 个工具项目计入社区模组。
- **修复旧待分类覆盖权威结果**：缓存或旧索引中的【待分类】不再覆盖身份目录中已经确认的主分类。
- **修复 GitHub 版本批次停滞**：Worker 改为根据上一次成功检查自动轮换仓库批次，避免某一定时任务漏跑时另一批模组长期停留在 Wiki 旧版本。
- **加固下载与身份边界**：保留精确 `owner/repo` 身份校验、市场仓库白名单、GitHub Release 路径限制、下载限流与 256 MiB 双端大小限制；异常或超限时安全切换为浏览器下载。
- **降低动态内容注入风险**：公告与市场动态文本使用安全转义和文本节点渲染，避免不可信内容进入可执行 HTML 上下文。
- **修复筛选后卡片操作失效**：筛选与排序时保留模组原始市场索引，确保更新、忽略、取消忽略等按钮始终作用于正确模组。
- **修复 ReadMe 说明文档图片加载失败与链接排版撕裂**：
  - 内置 Shields.io 离线矢量 SVG 生成引擎，静态徽章无需网络请求即可 100% 本地高保真渲染，消除中国大陆网络或断网离线环境下的原生破损裂图。
  - 增强复合图片超链接 `[![alt](img)](url)` 与独立超链接 `[text](url)` 解析，杜绝未闭合符号 `[` 与 `](...)` 散架裸露问题；连续徽章行自动聚合为弹性流式排布（Badge Row）。
  - 支持模组包（Zip）内置相对路径配图自动提取（Blob URL），模组本地说明截图可正常展示。
  - 完善图片加载失败容灾兜底机制（Fallback），动态徽章加载超时自动降级为文字状态徽章，正文大图失效转换为暗黑半透明提示卡片并附原图外链。
- **修复模组移动长按置顶/置底后释放误触二次上移**：
  - 重构按钮长按与短按手势状态机，引入局部按压态 `isPressed` 与全局长按锁定态 `_dolOptLongPressActive`；
  - 长按置顶/置底触发并重排界面后，释放鼠标时全局捕获并拦截目标按钮上的残留 `mouseup` 与 `click` 事件；
  - 彻底杜绝长按后在当前物理坐标新就位的模组被误判为短按上移的交互缺陷。
- **修复 SmartPhone Alpha 模组资料与作者识别混淆**：
  - 澄清并独立划分 Wiki 中【万能的智能手机】（NEEDMEET 遇欲等，ANLINSTUDIO 仓库）与【手机】（唐百玎HY，DOL-PhoneMod 仓库）两款不同的手机类模组；
  - 修正身份表与离线别名词库，剔除对 `HCPTangHY/DOL-PhoneMod` 及短别名“手机”的错误交叉关联；
  - 优化模组市场资料查询算法为“全局最高得分匹配”原则，确保 `SmartPhone Alpha` 准确显示《万能的智能手机》正版简介、作者团队与 GitHub 仓库，不再被靠前的相似模组拦截。

## v1.0.9.11 - 2026-09-20

### 1. 新功能
- **主分类与内容标签分离**：模组市场采用一个主分类配合最多三个内容标签，分类维度统一为框架与前置、剧情与角色、玩法与内容、界面与便利、外观与资源、规则与数值、修复与兼容。
- **动态分类筛选**：分类按钮根据当前市场数据自动生成并显示数量，新分类无需再修改固定按钮列表。

### 2. 优化性更新
- **权威分类随身份表下发**：已知模组的主分类与标签由身份表维护，并通过统一索引同步给游戏内市场和网站；未知模组使用保守规则推导。
- **待分类兜底**：无法可靠判断的新模组统一进入【待分类】，避免被错误塞入含义模糊的【其它模组】。
- **分类搜索联动**：市场搜索同时覆盖模组名称、作者、简介、主分类和内容标签。

### 3. 漏洞修复
- **修复依赖关系造成的分类误判**：简介中写有“依赖某框架”的内容模组不再被误标为框架模组。
- **修复宽泛关键词误判**：收紧“修复”“兼容”等规则，避免普通玩法说明或兼容性描述被错误识别为修复补丁。

## v1.0.9.10 - 2026-09-20

### 1. 优化性更新
- **MOD 管理器 Overlay 上下文隔离**：进入 MOD 管理器时会在重建标题与内容前显式锁定独立的 Overlay 身份，第三方模组可据此准确判断当前界面，避免跨页面注入。
- **通用第三方兼容方式**：MOD 管理器只显示自身声明的页签，自动隐藏后来插入的外部页签；不识别、不删除也不修改任何具体第三方模组节点，无需维护模组名称黑名单。
- **MOD 市场状态筛选反馈**：点击【社区收录】【本地已装】或【发现新版】后，对应统计卡片会保持高亮，当前筛选状态更加直观。
- **启动后自动刷新 MOD 市场**：所有 MOD 加载完成后在后台静默刷新一次市场索引并同步更新提示；进入市场直接复用结果，手动刷新仍保留。

### 2. 漏洞修复
- **修复日志扩展页签串入 MOD 管理器**：解决从日志等页面切换后，异步界面观察可能把【织境记忆】与【织境道具】错误追加到 MOD 管理器顶栏的问题。
- **修复 MOD 管理器样式上下文漂移**：确保 `T.currentOverlay` 与 DOM 的 `data-overlay` 始终同步为 `modloader`，避免专属顶栏布局因状态残留而失效。
- **修复重复导入时模组名称识别错误**：更新已安装模组时改用市场匹配结果、导入新增项及文件名别名确定目标，移除误取启用列表末项的兜底。
- **修复 MOD 市场简介被截断**：市场卡片统一高度，长简介可在卡片内部滚动完整查看，同时隐藏滚动条以保持界面整洁。

## v1.0.9.9 - 2026-09-20

### 1. 新功能
- **应用商店式原地下载进度**：点击【下载安装】或【一键更新】后，模组卡片会就地显示真实下载百分比；服务器未返回文件大小时自动切换为流动进度动画。
- **下载与安装状态联动**：操作按钮会依次显示正在下载、实时百分比与正在安装，完成后直接进入 ModLoader 导入流程，全程不离开当前页面。

### 2. 优化性更新
- **GitHub Release 流式下载通道**：通过现有 Cloudflare Worker 安全转发模组市场内的 GitHub Release 安装包，消除浏览器跨域限制导致的新页面跳转。
- **下载代理范围限制**：仅允许模组市场索引内仓库的标准 GitHub Release 安装包通过代理，拒绝任意外部地址，避免下载通道被滥用。

### 3. 漏洞修复
- **修复下载触发后跳转新页面**：移除 GitHub 安装包降级流程中的新标签页打开方式，失败回退改为当前页面内的隐藏下载通道。

## v1.0.9.8 - 2026-09-20

### 1. 优化性更新
- **统一版本索引同步**：模组市场现已与 Cloudflare 网站共用同一份自动版本索引，直接读取集中检查后的 GitHub 最新版本与新增 Wiki 模组。
- **多级网络回退**：统一索引不可达时依次使用浏览器最后成功缓存、Wiki 直连与内置别名映射，降低国内网络波动影响。
- **Cloudflare 身份字典接入**：模组市场会从 Cloudflare Pages 获取最新模组技术名、中文名、别名与仓库映射；后续补充识别规则无需重新发布模组。
- **离线兜底保留**：远程字典不可用时自动使用本地缓存或内置映射，不影响模组市场正常打开。

### 2. 漏洞修复
- **修复远程映射未参与安装识别**：远程补充的技术名与仓库名现在统一进入现有匹配流程，可正确显示安装状态与本地版本。

## v1.0.9.7 - 2026-09-20

### 1. 新功能
- **统一模组档案读取入口**：模组管理、模组说明与模组市场现在统一从 ModLoader 缓存中读取同名模组最后加载的有效档案，重复导入或更新后可立即显示正确版本。

### 2. 优化性更新
- **本地模组身份归一化**：技术名、`boot.json` 名称、多语言昵称和内置别名统一参与匹配，减少中英文名称不一致造成的漏识别。
- **织境空间别名补全**：补充 `AIStoryGen`、`织境空间` 与 `WovenRealm` 的精确映射，市场页可正确识别安装状态与本地版本。

### 3. 漏洞修复
- **重复导入不再重复计数**：已加载、已启用、已禁用及已安装列表统一按模组技术名去重，同一个模组重复导入不会继续抬高统计数字。
- **修复旧版本号残留**：重复导入新版模组后不再被旧缓存档案遮挡，模组管理、说明页与市场状态均显示最后加载版本。
- **修复启用状态交叉重复**：同一模组若异常同时出现在启用与禁用清单中，统一保留启用项并清理重复记录，避免统计和持久化状态分叉。

## v1.0.9.5 - 2026-09-17

### 1. 模组更新：方案 C 全域立体贯通感知与直升体系
- **顶部 Tab 胶囊数字气泡徽标**：
  - 无论玩家处于哪个界面，一旦检测到有模组可更新，顶部导航栏【模组市场】按钮动态呈现金色高亮胶囊角标（如 `[2]`）；无更新时自动销毁隐藏，保持界面极度清爽整洁。
- **【模组管理】首页置顶金边微光横幅**：
  - 玩家打开 ModLoader（默认落地模组管理页），一眼即见暗黑金边微光横幅，直观展示新版本数量；支持一键直达模组市场。
- **【模组管理】环境统计看板动态感知**：
  - 环境看板第 4 张卡片在检测到更新时动态从【ModLoader 版本】蜕变为金色呼吸闪烁的【发现新版 (N)】卡片，点击一秒直达市场并自动激活更新筛选。
- **【模组管理】本地列表原地一键升级**：
  - 本地列表中有新版本的模组条目，在副标题直观标注 `[可更新 -> vX.Y]`，并在操作按钮组最左侧直接提供金色高亮【更新】按钮；玩家无需跳转切页，原地点击即可弹出安装确认并直接拉取新版更新！
- **【模组市场】可更新模组强制最高优先级置顶**：
  - 模组市场中所有待更新模组不受排序规则干扰强制置顶在卡片列表最上方，并附带微光金边，一目了然。
  - 统计栏提供【一键全更 (N)】快捷通道，支持一键批量串行静默更新所有模组，并在全部完成后一次性弹出原生暗黑对话框询问重载游戏生效。

### 2. 修复与加固：模组市场已安装精准识别与版本号判定引擎重构
- **社区异常模组版本容错规则库（`MOD_MARKET_VERSION_RULES`）**：
  - **D.O.L.I 误报彻底根治**：针对作者发布 GitHub Release `v0.2.3` 时 zip 内部 `boot.json` 遗漏递增仍写为 `0.2.2` 的失误，建立专属容错映射，本地安装 `0.2.2` 即精准确认为已是最新，不再死循环误报更新。
  - **惠特尼剧情扩展 虚假更新彻底根治**：针对社区 Wiki 将旧搬运词条人工错填为 `v1.0`（作者仓库最高版本为 `0.3.1`）导致的断层误报，建立规则纠偏，本地安装 `0.3.1` 准确判定为已是最新，不再错误提示可更新。
- **用户自主权通道：支持【标为最新】与忽略特定版本更新**：
  - 针对未来不可预期的第三方作者打包失误或 Wiki 录入虚高问题，卡片操作栏新增【标为最新】按钮；
  - 玩家一键即可将该模组当前已安装状态确认为最新，不再受误报打扰，同时提供【恢复检测】支持随时撤回。
- **市场安装版本即时确权**：
  - 玩家在模组市场点击下载安装或一键全更成功后，系统自动在本地写入确权戳记，彻底防止因作者压缩包版本滞后导致的重复循环更新。
- **主名与子模块鉴别器（Compound Title & Sub-addon）双向约束**：
  - 彻底解决【织境空间-场景互动扩展】等同系列未安装子扩展错误对撞主模组（`AIStoryGen v0.1.606`）并误报“发现新版”的严重缺陷。
  - 市场复合模组若包含具体子扩展模块词，本地模组必须同时包含该子扩展关键词才允许命中，严禁主模组与子扩展互相串号。
- **通用停用词库过滤隔离**：
  - 引入 `GENERIC_STOP_WORDS` 停用词库，彻底杜绝单凭 `npc`、`mod`、`dol`、`扩展` 等 2-3 字通用词泛化误配至 `NPC Avatars Mod`。
- **版本号语义解析与呈现格式化**：
  - 加固语义化版本号解析器（`parseVersionNumbers`），支持自动剔除环境注释后缀（如 `(foodstuff-compat)`、`-for-dol-0.5.10`）与构建前缀；
  - 统一调用 `formatVersionDisplay` 渲染版本标签，彻底杜绝 `vv1.0.0` 重复前缀问题。
- **发布资源包多端智能优选**：
  - 在 GitHub Release 包含多个分端压缩包时，自动根据当前运行环境（手机/桌面端）智能优先匹配 `Mobile` 或 `PC` 包。

## v1.0.9.4 - 2026-09-17

### 1. 模组市场：游戏内无缝在线模组浏览与一键安装中心
- **全新游戏内【模组市场】选项卡与顺手排布**：
  - 在 ModLoader 管理器界面新增专属选项卡【模组市场】，**排布于【模组管理】左侧**（顺序为：通用 -> 模组市场 -> 模组管理 -> 模组说明 -> 加载日志），操作触手可及。
  - 严格遵循 DoL 原生暗黑文字冒险风格与 CSS 灰阶变量（`--850`、`--600` 等），彻底告别外挂感。
  - 数据直连 Miraheze Wiki 社区公开/私有模组索引，本地具备 2 小时智能缓存，并支持随时手动点击【刷新市场】拉取最新数据。
- **四重立体匹配引擎：精准识别已安装模组与本地版本**：
  - 彻底解决“Wiki 记录中文名 vs ModLoader 本地记录英文技术标识名”的识别断层问题，建立涵盖技术名、boot.json 别名、`dolOptGetModSubtext` 智能副标题、以及 GitHub 仓库名（Repo Name）的四重对撞机制。
  - 全量覆盖已加载模组、已启用旁加载模组及已禁用旁加载模组（`_dolOptModState`），识别率达 100%。
  - 自动与本地已安装模组比对语义化版本号（Semantic Versioning）：
    - `[未安装]`：显示主色【下载安装】按钮；
    - `[发现新版]`：金色高亮醒目标记，展示本地版本与市场新版，并提供直观的【一键更新】按钮；
    - `[已是最新]`：低调灰白呈现【已安装】状态，清晰标注 `已装: vX.X.X`；
    - `[外部资源]`：针对百度网盘、第三方论坛等无直链模组，提供【外部主页】快捷安全跳转。
- **国内加速镜像支持与免手动解压旁加载**：
  - 内置 GitHub 官方直连与国内高速加速镜像线路切换（`ghfast` / `ghproxy`），解决国内玩家网络超时与跨域 CORS 下载痛点。
  - 点击【下载安装】或【一键更新】后，系统自动通过二进制流拉取 Zip 资源，全自动注入 ModLoader 旁加载引擎并持久化写入 IndexedDB，免去手动下载压缩包、拖拽文件或解压覆盖的繁琐步骤。
  - 安装完成后唤起原生暗黑确认窗，支持一键重新载入游戏立刻生效。
- **智能分类筛选与实时全文搜索**：
  - 自动推导分类标签：【前置依赖】、【美化替换】、【服装配件】、【功能增强】、【数值便利】等胶囊快速过滤按钮。
  - 支持按模组名称、作者、简介关键词实时防抖检索；
  - 支持按状态（全部 / 可安装及可更新 / 仅看可更新 / 已安装）与排序（最新更新 / 模组名称）多维筛选。
- **窄屏与移动端第一优先适配**：
  - 严格恪守 0 Emoji 红线，采用纯文本徽标与高对比语义色彩；
  - 触控高度均 >= 32px，按钮设置防挤压保护，底部保留 60px 安全边距，彻底避免游戏底部悬浮栏遮挡。

### 2. 界面整合：全面合并通用页面至模组管理，重构 4 Tab 经典架构
- **精简 Tab 架构与默认直接落地**：
  - 彻底淘汰内容空旷、功能高度重叠的【通用】独立选项卡，打开 ModLoader 界面时**默认直接落地至【模组管理】主中枢**；
  - 顶部导航栏重归最工整的 4 选项卡布局：`【模组管理】` -> `【模组市场】` -> `【模组说明】` -> `【加载日志】`，每个按钮固定 120px，在电脑与手机屏幕上极其匀称整齐。
- **环境统计看板置顶**：
  - 将原本分散在通用页面的 4 个环境统计卡片（【已加载模组】、【已启用模组】、【已禁用模组】、【ModLoader 版本】）直接无缝置顶在【模组管理】最顶层；
  - 统计数字与模组启用、禁用、导入操作实时联动更新，全局概览一目了然。
- **全界面无感拖拽导入 (Full-Window Drag & Drop)**：
  - 彻底干掉原本笨重占地的“快捷添加模组”虚线方框；
  - 赋予整个【模组管理】容器原生全区域拖拽能力：从电脑桌面直接将 Mod Zip 拖入窗口任意位置即可松手导入，自动完成解包并弹出原生暗黑确认窗询问是否立即重新载入。
- **安全模式开关轻量化收拢**：
  - 在吸顶操作工具栏提示条右侧嵌入紧凑克制的 `[安全模式 (Safe Mode)]` 复选开关，排障随手可调，平时不抢占视线。
- **吸顶按钮左右顺手对调与重载询问全覆盖**：
  - 吸顶工具栏按钮顺序调整为：`【导入模组】` -> `【重新载入游戏】` -> `【智能整理模组与美化顺序】`，更符合“先导入、再重载”的使用逻辑；
  - 点击【导入模组】选择文件（单选或批量）导入后，统一弹出原生暗黑确认窗询问是否立即重新载入；选择“稍后重载”时自动刷新列表并高亮标出新模组，兼顾多模组连续添加。

## v1.0.9.3 - 2026-09-17

### 1. 美化管理：自动启用已启用旁加载模组的美化
- **美化状态智能跟随**：
  - 针对安装旁加载模组后美化未开启、或重新安装/更新模组导致美化被重置停用进而引发立绘缺失的痛点，新增美化自动激活机制。
  - 功能可选开启，**默认开启**；开启后，属于已启用旁加载模组的美化项将自动移至已启用列表并持久化保存。
- **防止误操作防呆保护**：
  - 开启状态下，跟随旁加载模组自动启用的美化项打上专属 `[自动启用]` 徽标，并将【禁用】按钮置灰锁定，无法手动停用，彻底避免漏开或误停用。
  - 保留覆盖图层优先级排序能力（拖拽排序、上下移动按键正常工作）。
  - 在游戏启动初始化时亦会静默对齐美化配置，确保开局即生效。

### 2. 模组日志：结构化排版与置顶智能诊断系统
- **高对比视觉重构**：
  - 彻底告别单调杂乱的纯文本日志，采用流式结构化行布局。
  - **时间戳**：采用低调微暗灰色弱化呈现，不抢占视觉重心；
  - **日志级别徽标**：`[错误]`（红框暗红底高对比）、`[警告]`（金框暗金底）、`[信息]`（低调灰蓝）分级明确；
  - **内容高亮**：错误行与警告行带有沉浸式微光底色与左侧彩色标识线。
- **自动提取报错模组与文件**：
  - 深度扫描日志正文，自动识别并置顶列出引发报错的模组名（如 `[ModName]`）与涉及的代码/剧情文件名（如 `.js`、`.twee`、`.json`）。
  - 点击置顶面板中的模组或文件徽章，可一键在日志搜索框填入并精准定位相关上下文。
- **常见错误通俗化总结与排查指南**：
  - 内置通俗化故障诊断知识库，针对普通玩家看不懂英文堆栈报错的痛点，自动匹配并展示大白话解析与修复步骤：
    - 前置依赖模组缺失（提示下载安装对应前置框架）；
    - 模组补丁冲突或文本不匹配（提示调整加载顺序或使用智能整理）；
    - 脚本语法错误或文件损坏（提示重新下载完整压缩包）；
    - 空指针未定义 TypeError（提示前置加载顺序或版本兼容性）；
    - 模组清单 boot.json 损坏或格式错误；
    - 浏览器 IndexedDB 本地存储超限 QuotaExceeded；
    - 图像资源缺失 404 等。

### 3. 通用管理：快捷模组添加与询问重载通道
- **极速快捷添加通道**：
  - 【通用】页签下的模组拖拽与文件添加区域升级为“快捷操作通道”。
  - 玩家将 Mod Zip 文件拖入通用页面（或点击选择文件）后，直接完成模组导入与美化自动对齐，随后弹出原生暗黑模态对话框询问是否立即重新载入游戏。
  - **立即重载**：点击后平滑重新载入游戏（`location.reload()`），使新模组立刻生效；
  - **稍后重载**：点击后保留在当前界面，提示模组已成功添加，允许玩家继续添加其他模组或调整设置，并在全部操作完毕后手动点击【重新载入游戏】或按 F5 刷新；
  - **交互习惯优化**：弹窗底部按钮全面调整为符合中文使用习惯的【左确认、右取消】布局（高亮金色【立即重载】居左，低调灰阶【稍后重载】居右）；
  - 【模组管理】顶部的【导入模组】按钮继续保留细致的智能分流与高亮浏览模式，满足不同场景需求。

### 4. 加载异常：启动自动弹窗与精准定位
- **错误主动感知与平滑定位**：
  - 游戏启动就绪时，自动检测本次加载过程是否存在 `Error` 级别严重报错。
  - 若检测到异常，直接自动展开模组管理器的【加载日志】窗口，并**自动平滑滚动定位到第一处错误所在行**，附带醒目的高亮呼吸脉冲。
- **随心开关控制**：
  - 该功能**默认开启**，方便第一时间发现模组异常；在【加载日志】页面的置顶诊断面板中提供快捷开关，开发者或高级玩家可根据需要自主关闭该自动弹窗。

### 5. 模组日志：点击“警告”或“错误”按钮无法跳转修复
- **结构化排版语义对齐**：
  - 此前日志页面的【错误】与【警告】统计按钮通过硬编码字符串检索 `"logWarning"`，由于结构化排版已将原始标签替换为中文徽标 `<span class="dol-opt-log-badge log-badge-warn">[警告]</span>`，导致纯文本检索未命中任何内容。
  - 重构为专属等级轮播定位器 `window.dolOptJumpToLevel(level)`，基于 DOM 类名（`.log-row-warn` 与 `.log-row-error`）精确定位；视口平滑居中滚动并触发金色/红色呼吸脉冲高亮；多条警告/错误支持点击循环切换。
  - 在全局搜索框中同步增加对 `logWarning`、`warning`、`警告`、`logError`、`error`、`错误` 等别名关键词的智能拦截分流，确保所有入口均能稳定跳转。

### 6. 模组管理：同组排序、长按置顶/置底与折叠箭头优化
- **同排序组统一管理与就地启闭**：
  - 彻底打破以往启用与禁用模组割裂展示且禁用项无法排序的限制，将已启用与已禁用的旁加载模组统一整合到同一个排序组中；
  - 禁用模组采用低调暗阶滤镜视觉呈现，但依然完全支持手柄拖拽排序与上下移动；
  - 模组的禁用与启用在**原位发生**，位置索引绝对锁定不变，彻底解决此前“测试时临时禁用模组再启用会被丢到末尾打乱排序”的痛点；
  - 引入 `dol_opt_sideload_mod_order` 本地缓存序列，重载或重启时完整还原启用与禁用的交错顺序；
  - 智能整理时将全量旁加载模组（包括已禁用模组）统一纳入拓扑排序依赖计算。
- **长按上移置顶 / 长按下移置底**：
  - 移动按钮引入长按手势监听（支持桌面鼠标与移动端触控，阈值 450ms）；
  - 短按移动一位，长按直接移动至最顶部（置顶）或最底部（置底），并附带丝滑反馈与提示；美化包列表同步支持该手势。
- **折叠面板标题降噪与 CSS 动态箭头**：
  - 移除已安装模组、美化图像包、内置核心模组等分组标题中多余重复的“（点击展开/收起）”文本；
  - 采用纯 CSS 几何金色三角形箭头（`::before`），展开时顺时针平滑旋转 90 度，风格契合原生暗黑文字冒险质感。
- **顶部操作栏固定吸顶与快捷重载游戏**：
  - 针对玩家调整模组或美化排序后仍需切回【通用】页面重启游戏的繁琐痛点，将模组管理页面的顶部提示条与标题操作栏整体设为吸顶固定（`position: sticky; top: 0`）；
  - 无论向下滚动多长的模组列表，操作栏始终置顶悬停并不遮挡内容；
  - 在置顶操作栏中新增高亮金色【重新载入游戏】快捷主按钮，调序或开关后随手一点即可直接重载游戏使改动生效；
  - 全面优化顶部提示文本，使表述更流畅自然、直观易懂（“智能整理将根据需要自动调整MOD的顺序；支持拖拽MOD进行手动调整；长按上下按钮可以将MOD置顶或置底。”）。

### 7. 工程与自动化验证
- **单元测试**：扩充 Node.js 自动化单元测试至 14 项，全面覆盖智能依赖排序、拖拽重排、美化防呆锁定、通用极速重启与询问弹窗、日志深度诊断与等级轮播跳转、同组排序保持、原地启闭与长按置顶置底、吸顶工具栏与重载游戏按钮校验，100% 通过。
- **规范校验**：严格执行全文件 Unicode 扫描，Emoji 数量严格为 0。

---

## v1.0.9.2 - 2026-09-17

### 1. 模组管理器拖拽排序与交互引擎
- **全平台拖拽排序 (Drag & Drop)**：
  - 为已启用旁加载模组列表与美化图像包列表加入专属拖拽手柄（`⋮⋮`）。
  - 支持桌面端鼠标 HTML5 拖拽与移动端单指触摸手柄拖拽，实时显示上下插入位置高亮指示条。
- **长列表边缘自动滚动 (Auto-scroll)**：
  - 彻底解决模组过多超出屏幕时无法跨屏拖动的体验痛点。
  - 设定 70px 视口边缘感应阈值，在指针靠近顶部或底部时，通过 `requestAnimationFrame` 驱动 2px~16px 动态缓动平滑自动滚屏。
  - 支持手势静止悬停持续平滑滚动，移出感应区或松手释放时立即安全停止并复位，杜绝内存泄漏与停不下来的情况。
  - 动态识别最近的滚动容器（如 `#customOverlayContent`），滚至物理边界时智能联动全局窗口。

### 2. 模组导入体验优化与智能分流
- **顶部常驻【导入模组】按钮**：
  - 在模组管理面板顶部显著位置新增主行动按钮，可直接点击调起系统文件选择器，支持批量或单选 Zip 模组包。
- **单模组与批量模组智能分流**：
  - **单模组导入**：若模组内包含有效的 ReadMe 说明文档，导入后自动平滑跳转至【模组说明】页签；若无 ReadMe，则平稳停留在【模组管理】界面并以金色脉冲呼吸光效高亮新模组。
  - **批量模组导入**：坚决不强行切走页面打断玩家操作流程，批量导入的模组在【模组管理】界面集中保持金色呼吸高亮，并弹出友好的导入汇总统计。

### 3. 模组禁用/启用与删除逻辑修复
- **解决接口报错**：
  - 彻底修复此前调用 `gui.overwriteModIndexDBModList is not a function` 的 TypeError 报错。
  - 深入 ModLoader 底层架构，改用 `ModLoadController`（多层寻址 `window.modModLoadController`、`modSC2DataManager.getModLoadController()`、`gui.modModLoadController`）执行 IndexDB 旁加载模组状态的持久化重写。
- **空指针与健壮性防护**：
  - 对 `gui?.gModUtils?.getMod` 引入全面的链式安全可选链保护，避免在特殊运行环境或缺少 gModUtils 实例时发生崩溃。

### 4. 游戏原生暗黑模态对话框系统
- **告别浏览器原生白色刺眼弹窗**：
  - 全面淘汰浏览器系统原生 `confirm()` 与 `alert()`，打造百分百融入 Degrees of Lewdity 原生暗黑文字冒险风格的模态对话框系统（`window.dolOptConfirm` 与 `window.dolOptAlert`）。
- **沉浸式视觉与动效**：
  - **遮罩层**：75% 纯黑高对比度半透明背景，叠加 `backdrop-filter: blur(2px)` 局部毛玻璃微模糊，层级设定为 `z-index: 100000` 绝对置顶。
  - **弹窗面板**：采用游戏原生深灰基底 `var(--850)` 与边框 `var(--600)`，搭配 `0 10px 30px rgba(0, 0, 0, 0.85)` 沉浸阴影与微缩放平滑进场动画。
  - **语义标题**：常规操作采用金色高亮（`.gold`），模组删除采用危险警示暗红（`.red`）。
  - **操作按钮**：主操作采用金框发光按钮，危险操作采用红框暗红按钮，取消操作采用低调灰阶按钮。
- **键盘无障碍交互**：
  - 打开弹窗后自动聚焦主确认按钮；
  - 支持键盘 `Enter` 键直接确认，支持 `Escape` 键随时取消；
  - 点击遮罩外部空白区域自动安全关闭。

### 5. 模组管理列表信息降噪与中文映射
- **去除技术废话**：全面清理模组条目中无用的“| 旁加载”标识。
- **主流模组友好中文别名**：针对作者未在 `boot.json` 中声明中文名的主流 Mod（如 `ModI18N`、`AutoClean`、`AutoClothesRepair`、`cummilk`、`MoreFarmUpgrade`、`DomRobin` 等）建立内置中文映射与职能说明，一目了然。

### 6. 工程与自动化保证
- **单元测试**：全套 Node.js 单元测试覆盖智能排序、拖拽重排、导入分流、存储持久化、模态框交互与版本校验，100% 通过。
- **规范校验**：全量代码与文本严格执行 Unicode 字符扫描，Emoji 数量严格为 0。
